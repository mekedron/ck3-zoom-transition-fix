# Overrides game/gfx/map/map_object_data/game_object_layers.txt
#
# Vanilla drops activities, buildings and units all on zoom step 9, the same
# step that unloads the three tree layers, the coast foam and the mountain
# env effects, hides the fort icons, swaps the map names and starts the realm
# colour overlay. Everything is torn down in one frame.
#
# These three move off that step:
#
#   layer             vanilla  here   camera height
#   activities_layer        9     7   295
#   building_layer          9    10   453
#   unit_layer              9    10   453
#
# Holdings go on step 10, where the realm colour overlay starts filling (this
# mod moves the start there from 9): the first colour shows, and one more
# click out takes the castles and towns away. 11 was tried and felt one click
# too late; 8 (1.0.0) was two clicks too early. A layer pushed higher stays
# visible over a wider zoom range and the visible map area grows fast as you
# pull back, so 10 is also where the frame cost is still fine. The AGOT patch
# uses 12 on a map with fewer holdings.

layer={
	name="activities_layer"
	fade_in=0
	fade_out=7
	category=""
	masks=""
	visibility_tags=""
}
layer={
	name="building_layer"
	fade_in=0
	fade_out=10
	category=""
	masks=""
	visibility_tags=""
}
layer={
	name="unit_layer"
	fade_in=0
	fade_out=10
	category=""
	masks=""
	visibility_tags=""
}
